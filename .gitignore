# Dependencias
node_modules/
# package-lock.json SI se versiona: `npm ci` lo necesita en el pipeline.

# Cobertura y build
coverage/
dist/
build/
*.log

# Entorno
.env
.env.*
!.env.example

# IaC
.terraform/
*.tfstate
*.tfstate.*
.terraform.lock.hcl
cdk.out/
packaged-*.yaml

# Sistema operativo
.DS_Store
Thumbs.db

# Editores
.idea/
*.swp

# Kiro: la configuracion de steering y hooks SI se versiona (es del equipo).
# Solo se ignora estado local de sesiones.
.kiro/cache/

# Material solo para el facilitador: NO se publica en el repo de asistentes.
# El archivo sigue existiendo localmente para que el facilitador lo estudie.
docs/FACILITADOR.md
